spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.klearn: DEBUG
    org.springframework: DEBUG
    org.hibernate.SQL: DEBUG
